import { VisibleDocumentDirective } from './visible-document.directive';

describe('VisibleDocumentDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleDocumentDirective();
    expect(directive).toBeTruthy();
  });
});
